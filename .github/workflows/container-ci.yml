name: container-ci

on:
  push:
    branches: [main]
    paths:
      - .dockerignore
      - .github/workflows/container-ci.yml
      - Dockerfile
      - Dockerfile.dockerignore
      - compose.yaml
      - docker/**
      - sdk/typescript/**
  pull_request:
    paths:
      - .dockerignore
      - .github/workflows/container-ci.yml
      - Dockerfile
      - Dockerfile.dockerignore
      - compose.yaml
      - docker/**
      - sdk/typescript/**
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  container:
    name: linux-amd64
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Verify customer build excludes secrets and scan data
        shell: bash
        run: |
          set -euo pipefail
          ignore_file=.dockerignore
          if [[ -f Dockerfile.dockerignore ]]; then
            ignore_file=Dockerfile.dockerignore
            if ! cmp -s .dockerignore "$ignore_file"; then
              echo "Dockerfile-specific ignore rules must match the approved customer baseline." >&2
              exit 1
            fi
          fi

          if grep -Fq '!' "$ignore_file"; then
            echo "Customer Docker ignore rules must not re-include excluded files: $ignore_file" >&2
            exit 1
          fi

          for pattern in \
            '**/.env' \
            '**/.env.*' \
            '**/.npmrc' \
            '**/node_modules' \
            '**/*.csv' \
            '**/repositories.csv' \
            '**/results' \
            '**/security-scans' \
            '**/state'; do
            if ! grep -Fxq -- "$pattern" "$ignore_file"; then
              echo "Missing customer Docker build exclusion in $ignore_file: $pattern" >&2
              exit 1
            fi
          done

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Build customer container
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: .
          load: true
          platforms: linux/amd64
          push: false
          tags: copilot-security:ci

      - name: Verify bundled scanner
        run: |
          docker run --rm copilot-security:ci --version
          docker run --rm copilot-security:ci bulk-scan --help
          docker run --rm copilot-security:ci info --json
          docker run --rm --entrypoint copilot copilot-security:ci --version

      - name: Validate hardened customer Compose configuration
        env:
          COPILOT_SECURITY_IMAGE: copilot-security:ci
        run: |
          mkdir -p results state
          chmod 700 results state
          printf 'id,repository,revision\n' > repositories.csv
          COPILOT_SECURITY_USER="$(id -u):$(id -g)"
          export COPILOT_SECURITY_USER
          docker compose config --quiet
          docker compose run --rm copilot-security --version

      - name: Reject interactive repository discovery
        shell: bash
        run: |
          set -euo pipefail
          if output="$(docker run --rm -t copilot-security:ci bulk-scan 2>&1)"; then
            echo "The customer container must require a repository CSV." >&2
            exit 1
          else
            status=$?
          fi
          if [[ "$status" -ne 2 ]]; then
            printf 'The customer container returned unexpected exit status %s:\n%s\n' "$status" "$output" >&2
            exit 1
          fi
          expected='copilot-security: bulk-scan requires a repository CSV; interactive discovery is not supported in this image.'
          if [[ "${output//$'\r'/}" != "$expected" ]]; then
            printf 'The customer container returned an unexpected repository discovery error:\n%s\n' "$output" >&2
            exit 1
          fi

      - name: Verify host-scoped noninteractive Git credentials
        shell: bash
        run: |
          set -euo pipefail
          docker run --rm \
            --entrypoint /bin/sh \
            --env GH_TOKEN=SYNTHETIC_GITHUB_TOKEN \
            copilot-security:ci \
            -ec 'actual="$(printf "protocol=https\nhost=github.com\n\n" | /usr/local/bin/copilot-security-git-credential get)"; test "$actual" = "$(printf "username=x-access-token\npassword=SYNTHETIC_GITHUB_TOKEN")"; test -z "$(printf "protocol=https\nhost=untrusted.example\n\n" | /usr/local/bin/copilot-security-git-credential get)"'
