{
  "schemaVersion": "1.0",
  "thresholds": {
    "minCompletionRate": 1,
    "minPrecision": 1,
    "minRecall": 1,
    "minF1": 1,
    "minCasePassRate": 1,
    "minNegativeCasePassRate": 1,
    "minStableDetectionRate": 1,
    "minValidationRate": 1,
    "minAttackPathRate": 1,
    "minCodeEvidenceRate": 1,
    "minSeverityAccuracy": 1,
    "maxFalsePositivesPerRun": 0
  },
  "cases": [
    {
      "id": "python-flask-builtin-str-open-redirect",
      "description": "A live Python built-in str conversion preserves a Flask query value that is emitted as an attacker-selected Location.",
      "fixture": "fixtures/python-flask-builtin-str-open-redirect",
      "findingsPaths": [
        "python-flask-builtin-str-open-redirect/run-1/findings.json"
      ],
      "expected": [
        {
          "id": "python-flask-open-redirect",
          "cwe": ["CWE-601"],
          "locations": [
            {
              "path": "src/server.py",
              "startLine": 9,
              "endLine": 9,
              "lineTolerance": 1
            }
          ],
          "acceptableSeverities": ["high", "medium"],
          "requireValidation": true,
          "requireAttackPath": true,
          "requireCodeEvidence": true,
          "requiredValidationTextAnyOf": [
            ["Flask route", "app.get", "GET /continue"],
            ["request.args", "query string", "next"],
            ["built-in str", "str(object)", "string conversion"],
            ["live binding", "unshadowed", "Python built-in"],
            ["flask.redirect", "Location header", "redirect target"],
            [
              "immutable allowlist",
              "same-origin allowlist",
              "destination restriction"
            ],
            ["CWE-601", "open redirect"]
          ],
          "requiredAttackPathTextAnyOf": [
            ["GET /continue", "Flask route", "app.get"],
            ["request.args", "next", "query value"],
            ["str(request.args", "built-in str", "string conversion"],
            ["redirect(target", "flask.redirect", "redirect helper"],
            ["Location", "absolute URL", "external origin"],
            [
              "attacker.invalid",
              "attacker-selected origin",
              "campaign=flask-builtin-str"
            ],
            ["follow_redirects=False", "does not follow", "no external request"]
          ],
          "forbiddenText": [
            "str sanitizes URLs",
            "string conversion validates the destination",
            "a shadowed str is the Python built-in",
            "a rebound builtins alias is trusted",
            "the witness contacted attacker.invalid",
            "every Flask redirect is vulnerable",
            "the host is fully compromised"
          ]
        }
      ]
    },
    {
      "id": "python-flask-builtin-str-allowlist-safe-redirect",
      "description": "The topology-matched control applies the same live built-in str conversion but redirects only immutable allowlist members and otherwise selects a fixed local fallback.",
      "fixture": "fixtures/python-flask-builtin-str-allowlist-safe-redirect",
      "findingsPaths": [
        "python-flask-builtin-str-allowlist-safe-redirect/run-1/findings.json"
      ],
      "expected": []
    }
  ]
}
