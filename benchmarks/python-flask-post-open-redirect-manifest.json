{
  "schemaVersion": "1.0",
  "thresholds": {
    "minCompletionRate": 1,
    "minPrecision": 1,
    "minRecall": 1,
    "minF1": 1,
    "minCasePassRate": 1,
    "minNegativeCasePassRate": 1,
    "minStableDetectionRate": 1,
    "minValidationRate": 1,
    "minAttackPathRate": 1,
    "minCodeEvidenceRate": 1,
    "minSeverityAccuracy": 1,
    "maxFalsePositivesPerRun": 0
  },
  "cases": [
    {
      "id": "python-flask-post-open-redirect",
      "description": "A Flask request.form value on an exact POST route receives only a root slash prefix and becomes a scheme-relative attacker-selected Location through the official redirect helper.",
      "fixture": "fixtures/python-flask-post-open-redirect",
      "findingsPaths": ["python-flask-post-open-redirect/run-1/findings.json"],
      "expected": [
        {
          "id": "python-flask-open-redirect",
          "cwe": ["CWE-601"],
          "locations": [
            {
              "path": "src/server.py",
              "startLine": 10,
              "endLine": 10,
              "lineTolerance": 1
            }
          ],
          "acceptableSeverities": ["high", "medium"],
          "requireValidation": true,
          "requireAttackPath": true,
          "requireCodeEvidence": true,
          "requiredValidationTextAnyOf": [
            ["Flask route", "app.route", "POST /continue"],
            ["methods=[\"POST\"]", "POST method", "static method list"],
            ["request.form", "form field", "form parameter"],
            ["flask.redirect", "official redirect"],
            ["Location header", "redirect location"],
            ["root-only prefix", "scheme-relative", "double slash"],
            ["fixed local prefix", "allowlist", "same-origin"],
            ["CWE-601", "open redirect"]
          ],
          "requiredAttackPathTextAnyOf": [
            ["POST /continue", "Flask route", "app.route"],
            ["request.form", "next", "form value"],
            ["root-only prefix", "\"/\" + target", "double slash"],
            ["flask.redirect", "redirect helper"],
            ["Location", "redirect target"],
            ["attacker.invalid", "attacker-selected origin", "external origin"],
            ["follow_redirects=False", "does not follow", "no external request"]
          ],
          "forbiddenText": [
            "the witness contacted attacker.invalid",
            "proves the endpoint is publicly reachable",
            "proves credentials were stolen",
            "every Flask form redirect is vulnerable",
            "a fixed local prefix is an open redirect",
            "the host is fully compromised"
          ]
        }
      ]
    },
    {
      "id": "python-flask-post-safe-local-redirect",
      "description": "The topology-matched Flask POST control percent-encodes the same hostile form value beneath a non-root fixed local redirect target.",
      "fixture": "fixtures/python-flask-post-safe-local-redirect",
      "findingsPaths": [
        "python-flask-post-safe-local-redirect/run-1/findings.json"
      ],
      "expected": []
    }
  ]
}
