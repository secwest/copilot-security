{
  "schemaVersion": "1.0",
  "thresholds": {
    "minCompletionRate": 1,
    "minPrecision": 1,
    "minRecall": 1,
    "minF1": 1,
    "minCasePassRate": 1,
    "minNegativeCasePassRate": 1,
    "minStableDetectionRate": 1,
    "minValidationRate": 1,
    "minAttackPathRate": 1,
    "minCodeEvidenceRate": 1,
    "minSeverityAccuracy": 1,
    "maxFalsePositivesPerRun": 0
  },
  "cases": [
    {
      "id": "python-fastapi-open-redirect",
      "description": "A FastAPI query string crosses an exact Annotated[str, Query()] boundary and is selected as the absolute Location URL by an official RedirectResponse.",
      "fixture": "fixtures/python-fastapi-open-redirect",
      "findingsPaths": ["python-fastapi-open-redirect/run-1/findings.json"],
      "expected": [
        {
          "id": "python-fastapi-open-redirect",
          "cwe": ["CWE-601"],
          "locations": [
            {
              "path": "src/redirects.py",
              "startLine": 5,
              "endLine": 5,
              "lineTolerance": 1
            }
          ],
          "acceptableSeverities": ["high", "medium"],
          "requireValidation": true,
          "requireAttackPath": true,
          "requireCodeEvidence": true,
          "requiredValidationTextAnyOf": [
            ["FastAPI path operation", "FastAPI route", "app.get"],
            ["Annotated[str, Query()]", "Query parameter", "query string"],
            ["RedirectResponse", "official redirect response"],
            ["url argument", "redirect location", "Location header"],
            [
              "absolute URL",
              "attacker-controlled origin",
              "attacker-controlled host"
            ],
            ["fixed local prefix", "allowlist", "same-origin"],
            ["CWE-601", "open redirect"]
          ],
          "requiredAttackPathTextAnyOf": [
            ["GET /login", "FastAPI route", "app.get"],
            ["next_url", "query parameter", "request query"],
            ["destination", "local alias", "alias"],
            ["RedirectResponse", "url argument"],
            ["Location", "redirect target"],
            [
              "attacker.invalid",
              "attacker-controlled origin",
              "external origin"
            ],
            ["follow_redirects=False", "does not follow", "no external request"]
          ],
          "forbiddenText": [
            "the witness contacted attacker.invalid",
            "proves the endpoint is publicly reachable",
            "proves credentials were stolen",
            "every redirect is vulnerable",
            "a fixed local prefix is an open redirect",
            "the host is fully compromised"
          ]
        }
      ]
    },
    {
      "id": "python-fastapi-safe-local-redirect",
      "description": "The topology-matched control percent-encodes the same hostile absolute URL beneath a fixed local /continue redirect target.",
      "fixture": "fixtures/python-fastapi-safe-local-redirect",
      "findingsPaths": [
        "python-fastapi-safe-local-redirect/run-1/findings.json"
      ],
      "expected": []
    }
  ]
}
