{
  "schemaVersion": "1.0",
  "thresholds": {
    "minCompletionRate": 1,
    "minPrecision": 1,
    "minRecall": 1,
    "minF1": 1,
    "minCasePassRate": 1,
    "minNegativeCasePassRate": 1,
    "minStableDetectionRate": 1,
    "minValidationRate": 1,
    "minAttackPathRate": 1,
    "minCodeEvidenceRate": 1,
    "minSeverityAccuracy": 1,
    "maxFalsePositivesPerRun": 0
  },
  "cases": [
    {
      "id": "node-multi-hop-tmp-path-traversal",
      "description": "An Express query prefix crosses three wrappers into tmp 0.2.5 fileSync, escapes the intended temporary root, and places protected export content in the adjacent directory.",
      "fixture": "fixtures/node-multi-hop-tmp-path-traversal",
      "findingsPaths": [
        "node-multi-hop-tmp-path-traversal/run-1/findings.json"
      ],
      "expected": [
        {
          "id": "node-tmp-prefix-path-traversal",
          "cwe": ["CWE-22"],
          "locations": [
            {
              "path": "src/storage.js",
              "startLine": 5,
              "endLine": 5,
              "lineTolerance": 1
            }
          ],
          "acceptableSeverities": ["high", "medium"],
          "requireValidation": true,
          "requireAttackPath": true,
          "requireCodeEvidence": true
        }
      ]
    },
    {
      "id": "node-multi-hop-patched-tmp",
      "description": "The matched Express source, wrappers, creator API, option position, and export write pin tmp 0.2.6, which rejects relative prefix components before creation.",
      "fixture": "fixtures/node-multi-hop-patched-tmp",
      "findingsPaths": ["node-multi-hop-patched-tmp/run-1/findings.json"],
      "expected": []
    }
  ]
}
