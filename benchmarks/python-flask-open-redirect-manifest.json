{
  "schemaVersion": "1.0",
  "thresholds": {
    "minCompletionRate": 1,
    "minPrecision": 1,
    "minRecall": 1,
    "minF1": 1,
    "minCasePassRate": 1,
    "minNegativeCasePassRate": 1,
    "minStableDetectionRate": 1,
    "minValidationRate": 1,
    "minAttackPathRate": 1,
    "minCodeEvidenceRate": 1,
    "minSeverityAccuracy": 1,
    "maxFalsePositivesPerRun": 0
  },
  "cases": [
    {
      "id": "python-flask-open-redirect",
      "description": "A Flask request.args query value receives only a root slash prefix and becomes a scheme-relative attacker-selected Location through the official redirect helper.",
      "fixture": "fixtures/python-flask-open-redirect",
      "findingsPaths": ["python-flask-open-redirect/run-1/findings.json"],
      "expected": [
        {
          "id": "python-flask-open-redirect",
          "cwe": ["CWE-601"],
          "locations": [
            {
              "path": "src/server.py",
              "startLine": 9,
              "endLine": 9,
              "lineTolerance": 1
            }
          ],
          "acceptableSeverities": ["high", "medium"],
          "requireValidation": true,
          "requireAttackPath": true,
          "requireCodeEvidence": true,
          "requiredValidationTextAnyOf": [
            ["Flask route", "app.get", "GET /continue"],
            ["request.args", "query string", "query field"],
            ["flask.redirect", "official redirect"],
            ["Location header", "redirect location"],
            ["root-only prefix", "scheme-relative", "double slash"],
            ["fixed local prefix", "allowlist", "same-origin"],
            ["CWE-601", "open redirect"]
          ],
          "requiredAttackPathTextAnyOf": [
            ["GET /continue", "Flask route", "app.get"],
            ["request.args", "next", "query value"],
            ["root-only prefix", "\"/\" + target", "double slash"],
            ["flask.redirect", "redirect helper"],
            ["Location", "redirect target"],
            ["attacker.invalid", "attacker-selected origin", "external origin"],
            ["follow_redirects=False", "does not follow", "no external request"]
          ],
          "forbiddenText": [
            "the witness contacted attacker.invalid",
            "proves the endpoint is publicly reachable",
            "proves credentials were stolen",
            "every Flask redirect is vulnerable",
            "a fixed local prefix is an open redirect",
            "the host is fully compromised"
          ]
        }
      ]
    },
    {
      "id": "python-flask-safe-local-redirect",
      "description": "The topology-matched Flask control percent-encodes the same hostile value beneath a non-root fixed local redirect target.",
      "fixture": "fixtures/python-flask-safe-local-redirect",
      "findingsPaths": ["python-flask-safe-local-redirect/run-1/findings.json"],
      "expected": []
    }
  ]
}
