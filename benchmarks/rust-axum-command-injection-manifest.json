{
  "schemaVersion": "1.0",
  "thresholds": {
    "completionRate": 1,
    "precision": 1,
    "recall": 1,
    "f1": 1,
    "casePassRate": 1,
    "negativeControlPassRate": 1,
    "stableDetectionRate": 1,
    "validationCoverage": 1,
    "attackPathCoverage": 1,
    "codeEvidenceCoverage": 1,
    "severityAccuracy": 1,
    "maxFalsePositivesPerRun": 0
  },
  "cases": [
    {
      "id": "rust-axum-shell-command-injection",
      "fixture": "fixtures/rust-axum-shell-command-injection",
      "expected": [
        {
          "title": "Axum query value reaches a shell command string",
          "cwe": ["CWE-78", "CWE-88"],
          "acceptableSeverities": ["critical", "high"],
          "path": "src/handlers/diagnostics.rs",
          "line": 15,
          "lineTolerance": 2,
          "requireValidation": true,
          "requireAttackPath": true,
          "requireCodeEvidence": true,
          "requiredValidationTextAnyOf": [
            ["Query", "Axum query", "request query"],
            ["format!", "command_line", "formatted"],
            ["Command::new", "std::process::Command"],
            ["sh -c", "shell", "-c"]
          ],
          "requiredAttackPathTextAnyOf": [
            ["input.target", "query value", "request value"],
            ["format!", "command string", "shell grammar"],
            ["sh", "process", "Command"],
            ["stdout", "response", "returned"]
          ],
          "forbiddenText": [
            "Command::arg always invokes a shell",
            "proves the handler is publicly reachable",
            "the host is fully compromised",
            "all server data is exposed"
          ]
        }
      ]
    },
    {
      "id": "rust-axum-argv-command",
      "fixture": "fixtures/rust-axum-argv-command",
      "expected": []
    }
  ]
}
