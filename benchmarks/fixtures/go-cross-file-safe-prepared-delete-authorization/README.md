# Go cross-file principal-scoped prepared deletion

This control preserves the DELETE route, wrapper, prepared statement lifecycle,
attack object ID, and response behavior while adding an account predicate bound
to a context-derived authenticated principal. The offline driver proves that the
victim record survives the attack and the caller can still delete its own record.
