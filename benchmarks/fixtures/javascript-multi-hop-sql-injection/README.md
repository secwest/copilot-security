# Multi-hop raw SQL user lookup

An unauthenticated HTTP value crosses exported gateway and user-service relays
and a separate database wrapper. The database wrapper interpolates the value
into SQL syntax.
