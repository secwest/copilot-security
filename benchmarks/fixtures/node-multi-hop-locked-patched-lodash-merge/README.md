# Multi-hop lock-resolved patched Lodash recursive merge

The matched Express and three-relay topology reaches the same official `lodash.merge()` source position. Its nearest manifest uses `^4.17.0`, but the adjacent npm v3 lockfile repeats that declaration and resolves Lodash to patched 4.17.21. Exact lock resolution therefore excludes the historical vulnerable-version model.

The dependency-free witness models the patched dangerous-key boundary without installing third-party packages in the regression corpus.
