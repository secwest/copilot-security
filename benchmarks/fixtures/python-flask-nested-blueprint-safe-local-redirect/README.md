# Flask nested-Blueprint fixed-local redirect

This source-matched control keeps the same application, parent and child
Blueprints, route, hostile query value, and two registration boundaries. It
percent-encodes the value beneath the fixed local `/continue?next=`
destination, so the request cannot select another origin.

The witness disables redirect following, observes only the `Location` header,
and makes no external request.
