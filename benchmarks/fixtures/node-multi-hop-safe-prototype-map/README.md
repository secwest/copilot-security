# Nested Map preferences

The matched Express and wrapper topology stores attacker-selected namespaces and keys only in nested `Map` instances. Prototype property names remain ordinary keys and cannot modify `Object.prototype`.
