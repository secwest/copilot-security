# Ktor typed-resource live command-list argv control

A Ktor `@Resource` path value is initially present in a shell command, but the
live command list is cleared and rebuilt before execution. The effective command
is fixed `printf`, and the resource value is passed only as an ordinary argument.
The paired native witness proves that the inert environment marker is not
expanded.
