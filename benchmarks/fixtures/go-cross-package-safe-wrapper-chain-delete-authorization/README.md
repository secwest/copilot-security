# Go authorized cross-package object-wrapper deletion

This control preserves the route, attacker-selected invoice, imported service
and repository packages, aliases, SQL dispatch, response, and deterministic
driver. It adds only a context-principal account predicate. The victim survives
while an invoice owned by the authenticated account is still deleted through
the complete wrapper chain.
