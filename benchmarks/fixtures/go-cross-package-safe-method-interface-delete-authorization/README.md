# Go authorized method and interface object deletion

This control preserves the route, imported concrete service method, local
repository interface binding, aliases, SQL dispatch, response, and
deterministic driver. It adds only a context-derived account predicate. The
victim survives while an owned invoice still deletes through both method calls.
