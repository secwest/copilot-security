# Go authorized all-path constructor-branch object deletion

This control preserves both repository implementations, the pointer layer,
two aliases of the returned pointer, identical nested writes on both explicit
branches, scalar state, interface dispatch, SQL, and response behavior. It adds
only the authenticated-principal predicate, preventing cross-account deletion
through the exact all-path state join.
