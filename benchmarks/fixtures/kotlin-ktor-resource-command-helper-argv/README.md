# Ktor typed-resource ProcessBuilder command-helper argv control

This control preserves the same route, typed mutator call, retained builder,
launch, output, and response topology as the paired injection fixture. The
helper replaces the command with a fixed ordinary executable and places the
resource value only in a separate argument.
