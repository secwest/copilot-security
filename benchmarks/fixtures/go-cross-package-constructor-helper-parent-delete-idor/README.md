# Go constructor-helper parent object deletion

The imported service constructor obtains its pointer layer from an exact local
helper in a separate package file, then injects the selected primary repository
through a nested write. The deterministic witness proves that the attacker-
selected object reaches the unscoped primary deletion through the materialized
helper call, creation, alias, return, constructor binding, and receiver path.
