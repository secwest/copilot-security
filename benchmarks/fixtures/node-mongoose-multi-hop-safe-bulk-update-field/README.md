# Node Mongoose multi-hop fixed bulk update field

The complete JSON patch crosses the same three exact relative imports, but storage reads only `displayName` and places it beneath a fixed server-owned `$set` field in the nested bulk update document.
