# Python cross-file parameterized SQL

The Flask route crosses the same relative-import boundary, but the database
wrapper keeps the request value outside SQL grammar and binds it through the
driver's positional-parameter API.
