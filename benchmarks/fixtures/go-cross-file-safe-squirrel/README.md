# Go cross-file safe Squirrel control

The request value crosses the same function boundary as the vulnerable fixture but remains a bound argument after a fixed `Where` expression. The deterministic local Squirrel subset proves the attack string cannot change SQL grammar or expose the internal record.
