# Spring R2DBC DatabaseClient bound-parameter control

This control preserves the vulnerable fixture's controller, wrapper, reactive
execution stage, database, schema, and hostile input. It changes only the SQL
boundary: the statement is fixed and the request value reaches `bind` as data.
The bounded `mvn verify` witness proves that the injection-shaped value returns
no account.
