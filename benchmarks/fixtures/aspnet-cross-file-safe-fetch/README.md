# ASP.NET cross-file safe-fetch fixture

The request value is only an exact key into a server-owned map of complete
HTTPS URIs. Unknown keys fail before any network request, redirects are
disabled, and the same request deadline and response ceiling as the vulnerable
fixture keep the negative control focused on destination authorization.
