# Node Mongoose multi-hop bulk update-operator injection

The complete JSON patch crosses three exact relative imports and becomes the nested update document of a Mongoose `bulkWrite()` operation. An attacker can supply `$unset` and remove the account's MFA secret.
