# Ktor literal process argument

This topology-matched negative control uses the same Ktor query source,
`ProcessBuilder`, process launch, output read, and HTTP response. It selects a
fixed ordinary executable and passes the request value only as a distinct
argument, so no shell command language interprets the value.
