[package]
name = "rust-axum-tokio-shell-command-injection-witness"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
tokio = { version = "=1.53.1", features = ["macros", "process", "rt"] }
