# Node Mongoose multi-hop selector injection

The JSON body value may be an object such as `{ "$ne": null }`. It crosses three exact relative imports and becomes the value of a Mongoose query-selector field without a literal-value boundary.
