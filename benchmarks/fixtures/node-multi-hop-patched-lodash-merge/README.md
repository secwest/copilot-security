# Multi-hop patched Lodash recursive merge

The matched Express and three-relay topology reaches the same official `lodash.merge()` source position, but the nearest runtime manifest pins Lodash 4.17.21. The version proof therefore rejects the historical prototype-polluting merge model.

The dependency-free witness models the patched dangerous-key boundary without installing third-party packages in the regression corpus.
