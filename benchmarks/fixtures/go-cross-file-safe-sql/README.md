# Go cross-file parameterized SQL control

The HTTP value crosses the same package boundary but appears only as a separate `database/sql` placeholder argument. The deterministic executable witness proves that SQL metacharacters remain data while an allowed bound value still returns its intended record.
