# MCP v2 Node process environment-data control

This control passes the same option-looking bytes to the fixed Node child only through a non-special environment variable while `NODE_OPTIONS` remains fixed and empty.
