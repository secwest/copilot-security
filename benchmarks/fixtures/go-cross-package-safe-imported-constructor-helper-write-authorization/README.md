# Go imported constructor-helper field-write authorization control

The imported parent helper allocates an empty layer, aliases its pointer, and
injects the selected primary repository through a later exported interface
field write before returning the original pointer. The primary deletion is
bound to the authenticated account, so the deterministic witness proves that
an attacker cannot delete a victim invoice while preserving a valid owned
deletion. The unused archive implementation remains a dispatch decoy.
