# Axum literal argument control

This topology-identical control keeps the Axum query source, process builder,
captured output, and response. It selects a fixed executable and places the
request value only in a distinct argument after a fixed format string, so shell
metacharacters remain ordinary data.
