# Copilot SDK user-message control

This control preserves the same HTTP source, two relative module boundaries, client construction, session creation, and final request. The system message remains a fixed server-owned instruction, while both request values are sent only through the SDK's ordinary user-message channel.
