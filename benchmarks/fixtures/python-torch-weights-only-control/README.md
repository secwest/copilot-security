# Python PyTorch weights-only control

This control preserves the positive fixture's bounded Flask upload, relative
wrapper, byte budget, package version, and malicious checkpoint. Its only
parser change is patched `torch.load(..., weights_only=True)`, which must reject
the fixture-local callable before `src.effects.mark` can run.
