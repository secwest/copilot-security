# Python Unpickler JSON control

This control preserves the vulnerable fixture's Flask request-stream source,
relative wrapper, effect module, and bounded witness. It uses standard-library
JSON instead of constructing an executable pickle object graph.
