# Cross-package promoted-method authorization control

This control uses the same two-level promoted method path as the exploit, but the database mutation binds the object identifier to the authenticated account from request context. Its executable witness rejects a cross-account deletion and permits deletion of an owned invoice.
