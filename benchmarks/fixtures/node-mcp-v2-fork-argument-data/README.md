# MCP v2 fork ordinary argument control

This topology-matched control keeps the fixed forked module, IPC lifecycle, timeout, schema, helper, and response while `execArgv` remains fixed and MCP tool input is passed only as an ordinary module argument.
