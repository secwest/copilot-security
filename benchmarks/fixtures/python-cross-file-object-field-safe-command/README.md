# Python cross-file object-field safe command

This matched control keeps the Flask source, relative import, fresh
`SimpleNamespace`, attacker-controlled field assignment, field selection, and
`shell=True` sink. The hostile value is stored only in the unselected `audit`
field while the selected `value` field remains a fixed command.
