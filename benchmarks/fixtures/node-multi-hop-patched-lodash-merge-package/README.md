# Multi-hop patched standalone lodash.merge package

The matched Express and three-relay topology reaches the same direct `lodash.merge` call, but the nearest runtime manifest pins the separately versioned package to patched 4.6.2. Package-specific version proof therefore excludes the historical model.

The dependency-free witness models the patched dangerous-key boundary without installing the third-party package.
