# Go principal-scoped cross-package helper-chain deletion

This control preserves the route, mutation wrapper, transaction, imported
coordinator, second internal package, transaction alias, attack object ID,
execution, commit, and response while adding only an account predicate bound to
a context-derived authenticated principal. The offline driver proves the victim
survives and an owned deletion still commits through the complete cross-package
chain.
