# Spring Java fluent ProcessBuilder argv control

This topology-matched Spring MVC control retains the same request binding,
fluent unassigned `ProcessBuilder`, process execution, and output handling as
the paired exploit fixture. It fixes the executable to `printf` and passes the
request value only as one ordinary operating-system argument, so shell
metacharacters remain literal data.
