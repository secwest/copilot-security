# Python Joblib JSON control

This control preserves the positive fixture's bounded Flask upload, relative
wrapper, requirements, effect module, and malicious Joblib payload. It replaces
only the unsafe `joblib.load` boundary with schema-oriented `json.load`, which
must reject the pickle stream before the fixture-local callable can run.
