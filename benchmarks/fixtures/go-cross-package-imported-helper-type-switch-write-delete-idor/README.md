# Go imported helper type-switch deletion

The imported parent helper creates a value layer containing a pointer holder,
copies the value three times, then binds a type-switch guard directly from its
interface parameter. The `nil`, interface, and final default arms each write the
selected primary repository through a different copy, making initialization an
all-path invariant. The deterministic witness proves that the attacker-selected
object reaches the unscoped primary deletion. The archive store is an exact
dispatch decoy.
