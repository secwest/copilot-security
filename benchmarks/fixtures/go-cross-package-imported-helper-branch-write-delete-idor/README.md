# Go imported helper all-path branch deletion

The imported parent helper creates a value layer containing a pointer holder,
copies the layer twice, and writes the selected primary repository through a
different copy on each explicit branch. Both paths therefore establish the
same shared holder before the original value is returned. The deterministic
witness proves that the attacker-selected object reaches the unscoped primary
deletion. The unused archive store is an exact dispatch decoy.
