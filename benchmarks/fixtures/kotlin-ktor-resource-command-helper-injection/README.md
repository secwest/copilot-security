# Ktor typed-resource ProcessBuilder command-helper injection

A unique same-file typed helper replaces a retained `ProcessBuilder` command
with shell grammar derived from a Ktor `@Resource` path value. The native
witness uses one short-lived process and harmless fixed command text.
