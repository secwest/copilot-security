# Go pgx parameter-binding control

The matched HTTP value crosses the same package boundary but appears only after the fixed SQL string as a `$1` value. The deterministic API-compatible witness proves that the attack bytes remain one bound value while an allowed value still returns its intended record.
