# Go embedded-interface type-switch deletion

The imported helper composes its repository contract from a same-package
embedded interface, then converts it to a second contract which embeds an
aliased interface from another local package. Canonical expansion proves the
two complete method sets are assignable before every type-switch arm writes
the selected repository through a shared holder. The executable witness proves
that an attacker-selected invoice reaches the unscoped deletion.
