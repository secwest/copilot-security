# Go authorized nested constructor-field-write object deletion

This control preserves both repository implementations, construction of the
pointer layer before injection, pointer alias, nested field write, scalar state,
interface dispatch, SQL, and response. It adds only the authenticated principal
predicate, preventing cross-account deletion through the selected repository.
