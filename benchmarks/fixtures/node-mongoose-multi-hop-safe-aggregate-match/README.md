# Node Mongoose multi-hop safe aggregate match

The same request topology contributes only one scalar beneath a fixed `$match` field and exact `$eq`. The remaining pipeline and public projection are server-owned.
