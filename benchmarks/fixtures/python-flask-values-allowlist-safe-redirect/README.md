# Flask `request.values` allowlist control

This topology-matched control reads from the same official combined query/form
collection but redirects only exact members of an immutable server-owned
tuple. The witness disables redirect following, selects the fixed `/account`
fallback, and performs no external request.
