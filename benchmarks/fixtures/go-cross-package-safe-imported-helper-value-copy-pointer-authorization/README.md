# Go imported helper value-copy pointer authorization control

The imported parent helper creates a value layer containing a pointer holder,
copies the layer, injects the selected primary repository through the copied
value's shared holder, and returns the original value. The primary deletion is
bound to the authenticated account, so the deterministic witness proves victim
survival and successful owned deletion. The archive implementation remains an
exact dispatch decoy.
