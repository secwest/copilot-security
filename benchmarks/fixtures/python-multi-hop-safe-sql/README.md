# Python multi-hop parameterized SQL

The registered Flask route crosses the same public service relay, but the
database wrapper keeps the request value outside SQL grammar through native
SQLite positional parameter binding.
