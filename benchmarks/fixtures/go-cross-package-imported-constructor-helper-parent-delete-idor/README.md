# Go imported constructor-helper parent object deletion

The service constructor obtains its pointer layer from an exact exported helper
in another local-module package, then injects the selected primary repository
through the layer's exported interface field. The deterministic witness proves
that the attacker-selected object reaches the unscoped primary deletion through
the imported helper call, creation, aliases, return, constructor binding,
nested write, and receiver path. The unused archive implementation is a decoy.
