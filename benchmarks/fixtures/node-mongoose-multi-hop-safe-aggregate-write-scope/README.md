# Node Mongoose multi-hop safe aggregate write scope

The same topology contributes only one scalar beneath fixed `$match` equality. The marker field, `$merge` destination, identity key, and merge policy are all server-owned.
