# Multi-hop patched extend deep merge

The matched Express and three-relay topology reaches the same exact `extend(true, target, ...sources)` form, but the nearest runtime manifest pins patched 3.0.2. Package-specific version proof therefore excludes the historical model.

The dependency-free witness models the patched dangerous-key boundary without installing the third-party package.
