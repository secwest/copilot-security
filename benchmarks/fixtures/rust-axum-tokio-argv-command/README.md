# Axum Tokio literal argv control

This topology-matched control carries the same Axum query value through the
official Tokio process builder and executes it, but supplies the value only as
one argument to fixed `printf`. No shell or interpreter parses the value as
command grammar. The paired witness proves that the inert marker text remains
literal and performs no file, network, credential, persistence, or privilege
operation.
