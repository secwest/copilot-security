# Guarded extract-zip symlink extraction

This topology-identical control pins the same `extract-zip` 2.0.1 release, request source, three wrappers, destination, callback shape, and later read. Its `onEntry` callback throws when the central-directory Unix mode identifies a symlink, preventing link materialization before extraction.
