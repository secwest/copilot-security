# Payout approval

Customers can edit a pending payout through `updates.py` while separately
requesting approval through `payouts.py`. Approved payouts are sent by a
payment worker using the current database record.
