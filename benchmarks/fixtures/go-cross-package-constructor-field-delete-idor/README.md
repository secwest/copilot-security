# Go constructor and concrete-field object-authorization deletion

The DELETE handler obtains an imported service through an exact constructor.
The service method traverses a named concrete repository field and forwards the
attacker-selected invoice ID into a fixed parameterized deletion without
principal scope. The deterministic driver proves that the victim is deleted.
