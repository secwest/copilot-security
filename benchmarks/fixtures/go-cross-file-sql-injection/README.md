# Go cross-file SQL injection fixture

The HTTP query value crosses one same-package function boundary, is formatted into `database/sql` query text, and reaches a real `DB.QueryContext` dispatch. The executable witness uses a deterministic standard-library driver to prove that an injected predicate exposes an otherwise internal record.
