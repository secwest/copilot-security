# Node Mongoose multi-hop update-operator injection

The complete JSON patch crosses three exact relative imports and becomes a Mongoose update document. An attacker can supply `$unset` and remove the account's MFA secret.
