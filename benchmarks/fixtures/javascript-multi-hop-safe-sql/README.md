# Parameter-bound multi-hop user lookup

The HTTP value crosses an exported user service and a separate database
wrapper. The wrapper keeps the value outside SQL syntax through native
positional parameter binding.
