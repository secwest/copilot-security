# Parameter-bound multi-hop user lookup

The HTTP value crosses exported gateway and user-service relays and a separate
database wrapper. The wrapper keeps the value outside SQL syntax through
native positional parameter binding.
