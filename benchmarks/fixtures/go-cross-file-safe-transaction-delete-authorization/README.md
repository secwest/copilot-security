# Go cross-file principal-scoped committed deletion

This control preserves the route, wrapper, transaction, attack object ID,
execution, commit, and response while binding an account predicate to a
context-derived authenticated principal. The offline driver proves that the
victim survives the committed attack transaction and the caller can still
commit deletion of its own invoice.
