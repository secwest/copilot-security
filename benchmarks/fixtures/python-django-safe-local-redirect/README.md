# Django fixed-local redirect control

This topology-matched control percent-encodes the same hostile query value
beneath a non-root fixed local redirect target. It cannot create a
scheme-relative Location. The witness disables redirect following and performs
no external request.
