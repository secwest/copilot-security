# Patched PostCSS previous-map containment

This topology-identical control pins PostCSS 8.5.18, which confines default previous-map loads to the `from` directory. The source, three wrappers, process call, options, and returned map remain unchanged.
