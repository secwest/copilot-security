# Go type-alias interface authorization control

This control preserves every local and cross-package alias, embedded contract,
conversion, constructor, field, copy, request, and deterministic database
witness from the exploit. Its only security change is the authenticated account
predicate. The witness proves that a victim invoice survives while an invoice
owned by the authenticated account can still be deleted.
