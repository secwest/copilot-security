# Python cross-file safe dataclass field selection

This matched control keeps the Flask request, relative import, generated
standard-library dataclass, hostile field write, exact field selection, and
real `shell=True` sink. The hostile value is confined to the declared `audit`
field while the selected `value` field remains fixed, so the witness must not
create its disposable temporary marker.
