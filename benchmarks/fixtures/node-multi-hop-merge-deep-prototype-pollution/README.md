# Multi-hop vulnerable merge-deep package

An Express JSON object crosses three relative-import wrappers into a source operand of the direct callable exported by `merge-deep`. The nearest runtime manifest pins vulnerable 3.0.2, before the 3.0.3 `constructor.prototype` fix.

The dependency-free witness isolates the historical recursive traversal. The fixture never installs the vulnerable dependency; scanner acceptance separately requires the exact package binding, matching package-specific version evidence, source position, and request-to-sink flow.
