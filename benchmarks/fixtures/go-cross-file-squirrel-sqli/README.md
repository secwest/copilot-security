# Go cross-file Squirrel SQL injection fixture

The HTTP query value crosses one same-package function boundary, is formatted into a Squirrel `Where` fragment, and reaches `Query` only after the immutable builder receives a typed `database/sql` runner. The executable witness uses a deterministic local, signature-compatible Squirrel subset to prove that the injected predicate exposes an otherwise internal record without downloading dependencies.
