# FastAPI open redirect

An official FastAPI GET path operation binds an attacker-controlled query
parameter and passes it to the official `RedirectResponse` URL argument. The
pinned fixture crosses one explicit relative Python wrapper before that sink. The
pinned TestClient witness does not follow the redirect or contact the selected
origin; it proves only that an absolute attacker-selected URL is emitted in the
HTTP `Location` header.
