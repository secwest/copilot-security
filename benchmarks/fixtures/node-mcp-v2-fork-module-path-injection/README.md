# MCP v2 fork modulePath injection

This fixture places schema-validated MCP tool input in `child_process.fork`'s `modulePath` position. The bounded witness selects only the checked-in `selected-child.mjs` module and observes its inert IPC marker.
