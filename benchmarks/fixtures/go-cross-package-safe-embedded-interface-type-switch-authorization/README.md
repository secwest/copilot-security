# Go embedded-interface authorization control

This fixture preserves the exploit topology, same-package embedding, imported
aliased embedding, canonical method identities, copies, type switch, request,
and deterministic database witness. Its only security change is the
authenticated account predicate. The witness proves that the victim survives
while an invoice owned by the authenticated account can still be deleted.
