# Go type-alias interface deletion

The request path reaches a repository contract named through local aliases,
embedded through another alias, and converted through a cross-package alias
chain whose method signature also contains an exact type alias. Every type
identity is locally resolvable, but the final deletion is scoped only by the
attacker-selected invoice identifier. The executable witness proves that the
victim invoice is deleted.
