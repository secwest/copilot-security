# Node Mongoose multi-hop safe selector

This topology preserves the same request source and Mongoose sink while `$eq` forces the request object to be interpreted as a literal field value rather than query-selector grammar.
