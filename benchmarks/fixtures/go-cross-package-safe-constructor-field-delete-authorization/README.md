# Go authorized constructor and concrete-field object deletion

This control preserves the route, imported service constructor, concrete
repository field, aliases, SQL dispatch, response, and deterministic driver. It
adds only a context-derived account predicate. The victim survives while an
owned invoice still deletes through the constructor and field method path.
