# MCP v2 aliased Node argument-data control

This topology-matched control preserves the same module-scope `runtime = process.execPath` alias and places an exact `--` before MCP tool input. The fixed `--version` and `--help` values therefore remain ordinary script data.
