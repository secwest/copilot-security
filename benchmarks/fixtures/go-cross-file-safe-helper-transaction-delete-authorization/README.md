# Go principal-scoped helper-committed deletion

This control preserves the route, mutation wrapper, transaction, typed commit
helper, attack object ID, execution, commit, and response while adding only an
account predicate bound to a context-derived authenticated principal. The
offline driver proves the victim survives and an owned deletion still commits.
