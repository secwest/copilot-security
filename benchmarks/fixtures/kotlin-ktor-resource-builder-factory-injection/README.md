# Ktor typed-resource ProcessBuilder factory injection

A unique same-file helper returns a `ProcessBuilder` whose shell-command
argument comes from a Ktor `@Resource` path value. The native witness uses one
short-lived process and proves that fixed shell grammar interprets a harmless
constant command sequence.
