# Python multi-hop filesystem path traversal

A Flask query value crosses public gateway and service relays before a Python
filesystem wrapper joins it beneath a document root. Parent segments escape the
root and disclose a private file.
