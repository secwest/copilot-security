# Go all-path constructor-branch object deletion

The imported constructor creates the service and pointer layer, makes two
aliases of the returned pointer, and injects the selected primary repository
through the same nested field on both explicit branches. The deterministic
witness proves that the attacker-selected object reaches the unscoped primary
repository deletion after the exact all-path state join.
