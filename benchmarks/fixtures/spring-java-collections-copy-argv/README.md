# Spring Java Collections.copy argv control

This control keeps the same caller-owned three-element list,
`ProcessBuilder(List)` identity, `Collections.copy` rewrite, process lifecycle,
and request flow as the matched positive. It copies `printf`, `%s`, and the
request value as distinct argv elements, so command-language metacharacters are
printed as data rather than interpreted.
