# Ktor typed-resource ProcessBuilder factory argv control

This control preserves the same route, helper call, process launch, output, and
response topology as the paired factory-injection fixture. Its helper selects a
fixed ordinary executable and places the resource value only in a separate
argument, so no shell parses it.
