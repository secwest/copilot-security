# Multi-hop prototype pollution

An Express JSON patch crosses three relative-import wrappers before two attacker-selected property names index an ordinary object. The `__proto__` namespace reaches `Object.prototype`, and the second key installs an inherited authorization flag.
