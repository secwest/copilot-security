# Node multi-hop filesystem path traversal

An HTTP query value crosses exact gateway and service relays before an imported
Node filesystem wrapper resolves it beneath a document root. Parent segments
escape that root and disclose a private file.
