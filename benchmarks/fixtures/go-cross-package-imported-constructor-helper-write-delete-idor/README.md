# Go imported constructor-helper field-write deletion

The imported parent helper allocates an empty layer, aliases its pointer, and
injects the selected primary repository through a later exported interface
field write before returning the original pointer. The deterministic witness
proves that the attacker-selected object reaches the unscoped primary deletion
through the exact helper call, aliases, write, return, and receiver path. The
unused archive implementation is a dispatch decoy.
