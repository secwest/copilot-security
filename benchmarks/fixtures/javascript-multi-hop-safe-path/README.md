# Node multi-hop fixed document selection

The same HTTP, gateway, service, and filesystem topology maps the untrusted key
to one server-owned complete path. Parent segments cannot become filesystem
grammar and the private witness remains inaccessible.
