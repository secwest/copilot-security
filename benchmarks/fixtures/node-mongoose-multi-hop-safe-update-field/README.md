# Node Mongoose multi-hop fixed update field

The complete JSON patch crosses the same three exact relative imports, but storage reads only `displayName` and places it beneath a fixed server-owned `$set` field.
