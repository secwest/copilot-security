# Go imported helper all-path branch authorization control

The imported parent helper creates a value layer containing a pointer holder,
copies the layer twice, and writes the selected primary repository through a
different copy on each explicit branch. Both paths establish the same shared
holder before the original value is returned. The primary deletion is bound to
the authenticated account, so the deterministic witness proves victim survival
and successful owned deletion. The archive implementation remains an exact
dispatch decoy.
