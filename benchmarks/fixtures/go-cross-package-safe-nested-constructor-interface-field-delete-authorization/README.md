# Go authorized nested constructor-interface object deletion

This control preserves both implementations, the pointer layer, scalar fields,
constructor injection, interface dispatch, SQL execution, and HTTP response. It
adds only the authenticated principal predicate, preventing cross-account
deletion through the selected nested repository.
