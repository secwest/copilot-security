# Sails Action2 fixed-thumbnail control

The topology-matched Action2 controller still declares and receives `filename`,
but the filesystem sink uses the fixed server-owned thumbnail path. The witness
proves traversal-shaped input remains inert.
