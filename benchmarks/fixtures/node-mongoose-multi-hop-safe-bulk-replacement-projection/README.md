# Node Mongoose multi-hop fixed bulk replacement projection

The JSON document crosses the same three imports, but the replacement is constructed with fixed server-owned fields and reads only the request's `displayName` scalar.
