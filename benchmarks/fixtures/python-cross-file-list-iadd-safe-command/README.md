# Python cross-file list `+=` argv control

This topology-matched control sends the same hostile Flask value through the
same relative import and `+=` list mutation, but only as a literal argument to
a fixed executable. `shell=False` keeps metacharacters outside shell grammar.
