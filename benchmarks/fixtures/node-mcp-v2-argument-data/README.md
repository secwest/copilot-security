# MCP v2 Node interpreter argv control

This topology-matched control preserves the official MCP SDK, schema-bearing tool, callback, helper, fixed Node runtime, timeout, output capture, and response while inserting `--` before the tool-controlled argv element.

Run `npm install` and `npm run witness` with Node.js 22 or later. The witness proves both ordinary text and dash-prefixed runtime-option spellings remain inert script data.
