# Go authorized constructor-injected interface-field object deletion

This negative control preserves constructor injection, multiple implementations,
interface dispatch, SQL execution, and the HTTP response. It adds only the
authenticated principal predicate, which prevents cross-account deletion.
