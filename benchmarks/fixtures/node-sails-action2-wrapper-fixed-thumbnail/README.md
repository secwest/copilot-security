# Sails Action2 relative-wrapper fixed-path control

The explicitly routed Action2 receives the same traversal-shaped declared input
but the relative module selects one fixed server-owned thumbnail. The witness
performs no write or network operation.
