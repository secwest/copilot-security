# Ktor typed-resource literal command replacement

This topology-matched negative control keeps the typed resource, mutable
`ProcessBuilder`, command replacement, launch, output read, and response. The
effective command selects a fixed ordinary executable and passes the resource
value as one distinct argument, so no shell command language interprets it.
