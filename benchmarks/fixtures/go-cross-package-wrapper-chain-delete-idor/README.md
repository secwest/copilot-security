# Go cross-package object-wrapper deletion

The DELETE handler forwards an attacker-selected invoice through an imported
service package and then an imported repository package. The repository deletes
by object ID without principal scope. The deterministic offline driver proves
that the victim invoice is deleted through the complete wrapper chain.
