# Safe Go manual Cmd shell-command fixture

The same indexed HTTP source, wrapper, manual `exec.Cmd`, `Args` field
assignment, execution closure, and attack bytes are retained. The request can
only select an immutable, server-owned complete command, and unknown keys are
rejected before process construction.
