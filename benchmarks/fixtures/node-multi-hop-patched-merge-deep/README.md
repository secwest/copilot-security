# Multi-hop patched merge-deep package

The matched Express and three-relay topology reaches the same direct `merge-deep` call, but the nearest runtime manifest pins patched version 3.0.3. Package-specific version proof therefore excludes the historical model.

The dependency-free witness models the patched dangerous-key boundary without installing the third-party package.
