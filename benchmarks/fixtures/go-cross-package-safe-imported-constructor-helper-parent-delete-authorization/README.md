# Go imported constructor-helper parent object authorization control

The service constructor obtains its pointer layer from an exact exported helper
in another local-module package, then injects the selected primary repository
through the layer's exported interface field. The primary deletion is bound to
the authenticated account, so the deterministic witness proves that an attacker
cannot delete a victim invoice while preserving a valid owned deletion. The
unused archive implementation remains an exact dispatch decoy.
