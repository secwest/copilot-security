# Go nested constructor-field-write object deletion

The imported constructor creates the service and its pointer layer first, then
injects the selected repository through a nested field write on an alias. The
deterministic witness proves that the attacker-selected object reaches only the
selected primary repository deletion.
