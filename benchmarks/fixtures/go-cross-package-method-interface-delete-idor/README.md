# Go method and interface object-authorization deletion

The DELETE handler invokes an imported concrete service method. That method
binds an imported repository implementation to a local interface and forwards
the attacker-selected invoice ID into a fixed parameterized deletion without
principal scope. The deterministic driver proves that the victim is deleted.
