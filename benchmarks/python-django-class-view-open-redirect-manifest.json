{
  "schemaVersion": "1.0",
  "thresholds": {
    "minCompletionRate": 1,
    "minPrecision": 1,
    "minRecall": 1,
    "minF1": 1,
    "minCasePassRate": 1,
    "minNegativeCasePassRate": 1,
    "minStableDetectionRate": 1,
    "minValidationRate": 1,
    "minAttackPathRate": 1,
    "minCodeEvidenceRate": 1,
    "minSeverityAccuracy": 1,
    "maxFalsePositivesPerRun": 0
  },
  "cases": [
    {
      "id": "python-django-class-view-open-redirect",
      "description": "A registered Django View.as_view() GET handler adds only a root slash to request.GET data and sends the resulting scheme-relative authority through the official redirect helper.",
      "fixture": "fixtures/python-django-class-view-open-redirect",
      "findingsPaths": [
        "python-django-class-view-open-redirect/run-1/findings.json"
      ],
      "expected": [
        {
          "id": "python-django-open-redirect",
          "cwe": ["CWE-601"],
          "locations": [
            {
              "path": "src/views.py",
              "startLine": 9,
              "endLine": 9,
              "lineTolerance": 1
            }
          ],
          "acceptableSeverities": ["high", "medium"],
          "requireValidation": true,
          "requireAttackPath": true,
          "requireCodeEvidence": true,
          "requiredValidationTextAnyOf": [
            ["Django URL pattern", "urlpatterns", "path registration"],
            ["View.as_view", "class-based view", "ContinueView"],
            ["get(self, request)", "GET handler", "request.GET"],
            ["django.shortcuts.redirect", "official redirect"],
            ["Location header", "redirect location"],
            ["root-only prefix", "scheme-relative", "double slash"],
            ["CWE-601", "open redirect"]
          ],
          "requiredAttackPathTextAnyOf": [
            ["continue/", "Django URL pattern", "urlpatterns"],
            ["View.as_view", "ContinueView", "class-based view"],
            ["request.GET", "next", "query value"],
            ["root-only prefix", "\"/\" + target", "double slash"],
            ["django.shortcuts.redirect", "redirect helper"],
            ["Location", "attacker-selected origin", "attacker.invalid"],
            ["follow=False", "does not follow", "no external request"]
          ],
          "forbiddenText": [
            "the witness contacted attacker.invalid",
            "proves the endpoint is publicly reachable",
            "proves credentials were stolen",
            "every Django class-based view is vulnerable",
            "a fixed local prefix is an open redirect",
            "the host is fully compromised"
          ]
        }
      ]
    },
    {
      "id": "python-django-class-view-safe-local-redirect",
      "description": "The topology-matched Django View.as_view() control percent-encodes the same hostile query value beneath a non-root fixed local redirect target.",
      "fixture": "fixtures/python-django-class-view-safe-local-redirect",
      "findingsPaths": [
        "python-django-class-view-safe-local-redirect/run-1/findings.json"
      ],
      "expected": []
    }
  ]
}
